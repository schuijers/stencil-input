{
  "name": "@schuijers/stencil-input",
  "private": true,
  "version": "0.0.1",
  "description": "Stencil input component with client and server side validation.",
  "license": "MIT",
  "author": "Martijn Schuijers",
  "files": [
    "dist/"
  ],
  "repository": "https://github.com/schuijers/stencil-input.git",
  "dependencies": {
    "@stencil/core": "^0.6.1",
    "@stencil/router": "latest"
  },
  "devDependencies": {
    "@stencil/dev-server": "latest",
    "@stencil/utils": "latest",
    "@types/jest": "^22.2.0",
    "jest": "^22.4.2"
  },
  "scripts": {
    "build": "stencil build --prerender",
    "dev": "sd concurrent \"stencil build --dev --watch\" \"stencil-dev-server\" ",
    "serve": "stencil-dev-server",
    "start": "npm run dev",
    "test": "jest --no-cache --config jest.config.json",
    "test.watch": "jest --watch --no-cache --config jest.config.json"
  }
}
